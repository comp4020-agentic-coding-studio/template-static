# Brief

> This is the per-instance brief. It is the only part of the template that
> changes from one assignment or crit to the next --- replace it each time.

For this prototype you must:

- TODO: the actual requirements for this week go here.

How you'll be checked: the automated checks in `.github/workflows/checks.yml`,
plus the conformance test in `spec/spec.test.js`. The test asserts the
**contracts** below, not how you build them, so you're free to implement them
any way you like.

## Required contracts

- the home page has a primary navigation landmark
- the home page has exactly one top-level heading (`h1`)
- the element marked `data-testid="intro"` is present
